{
  "name": "galactic-panic",
  "version": "0.1.0",
  "private": true,
  "description": "The Galactic Panic band website (Eleventy, hosted on GitHub Pages)",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "eleventy --serve",
    "build": "eleventy",
    "clean": "rm -rf site/_site",
    "validate": "node scripts/validate-metadata.js",
    "test": "node --test",
    "test:coverage": "node --test --experimental-test-coverage"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.6",
    "@11ty/eleventy-img": "^6.0.4"
  }
}
