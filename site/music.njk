---
layout: base.njk
title: Music
permalink: /music/
---

<h1>Music</h1>
<p class="eyebrow">All releases, most recent first</p>

{% set songs = collections.songs %}

{% if songs.length > 0 %}
<div class="songs-grid">
  {% for song in songs %}
    <a href="/songs/{{ song.slug }}/" class="song-card">
      <div class="song-card-art">
        {% if song.cover_url %}<img src="{{ song.cover_url }}" alt="">{% endif %}
      </div>
      <div class="song-card-body">
        <div class="song-card-title">{{ song.title }}</div>
        <div class="song-card-meta">
          {% if song.latest_release_date %}{{ song.latest_release_date | prettyDate }}{% else %}Unreleased{% endif %}
          {% if song.has_pro_version %}<span class="song-card-tag">Spotify</span>{% endif %}
          {% if song.has_home_version and not song.has_pro_version %}<span class="song-card-tag">Bandcamp</span>{% endif %}
        </div>
      </div>
    </a>
  {% endfor %}
</div>
{% else %}
<div class="empty-state">
  No releases yet. Add a song folder under <code>content/songs/</code> with a metadata.json.
</div>
{% endif %}
